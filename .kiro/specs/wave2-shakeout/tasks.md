# Wave 2 Shakeout — Implementation Plan

## Tasks

- [ ] 1. Lane formatter, marker component, app mount, and a green gate
- [ ] 1.1 Build the shakeout surface across four files and leave the repository gate green. (a) `src/components/wave2-shakeout/formatLane.ts`: a module with no import statements exporting exactly one named export `export function formatLane(branch: string, taskId: string): string` and no default export — return the literal string `unknown` when `branch.trim()` is empty [req:1.2], return `unknown` when `taskId.trim()` is empty [req:1.3], otherwise return the template string `` `${branch.trim()}#${taskId.trim()}` `` [req:1.4]; hold no module-level mutable state and read no clock, random source, DOM, or network [req:1.5]. (b) `src/components/wave2-shakeout/formatLane.test.ts`: plain vitest tests (no DOM, no testing-library) asserting `formatLane('lane/x', '1.1')` is `lane/x#1.1` [req:4.1]; `formatLane('  lane/x  ', ' 1.1 ')` is `lane/x#1.1` [req:4.2]; and each of `formatLane('', '1.1')`, `formatLane('   ', '1.1')`, `formatLane('lane/x', '')`, `formatLane('lane/x', '   ')` is `unknown` [req:4.3]. (c) `src/components/wave2-shakeout/wave2-shakeout.tsx`: module-level `const LANE_BRANCH = 'lane/wave2-shakeout/1.1'` and `const LANE_TASK = '1.1'`, then a prop-less state-less `export function Wave2Shakeout()` whose entire render is `<span data-shakeout="wave2" className="hidden">{formatLane(LANE_BRANCH, LANE_TASK)}</span>` with `formatLane` imported from `./formatLane` — no props, no state, no event handlers, no interactive elements, no additional markup, and no inlined or duplicated formatting [req:2.1, 2.2, 2.3]. (d) `src/components/wave2-shakeout/wave2-shakeout.test.tsx`: mirror the import style of `src/components/drill-wave-c-twins/drill-wave-c-twins.test.tsx` (vitest + jsdom + testing-library, `cleanup()` in `afterEach`) and cover rendered text content equals exactly `lane/wave2-shakeout/1.1#1.1` [req:4.4]; the rendered `<span>` carries `data-shakeout="wave2"` and the class `hidden` [req:4.5]; and one test that does `import App from '../../App'`, renders `<App />`, and asserts `container.querySelectorAll('[data-shakeout="wave2"]')` has length 1 [req:4.6]. (e) `src/App.tsx`: add one import line `import { Wave2Shakeout } from './components/wave2-shakeout/wave2-shakeout'` in the existing alphabetical import block, mount `<Wave2Shakeout />` exactly once immediately after the existing `<DrillWaveCTwins />` mount [req:2.4], and remove the `debugger` statement that currently sits at the top of the `App` function body [req:3.1] — every other line of the file stays byte-identical. Add no npm dependency and edit no config file. The repository gate for this task is `npm run verify` (which runs oxlint, then `tsc -b`, then `vite build`) plus `npm test`: both must exit zero [req:3.2, 3.3]. A type-check and a test run alone are NOT the gate — oxlint is part of it and rejects the `debugger` statement.
  _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 2.4, 3.1, 3.2, 3.3, 4.1, 4.2, 4.3, 4.4, 4.5, 4.6_
  _Boundary: src/components/wave2-shakeout, src/App.tsx_

- [ ] 2. Lane counter
- [ ] 2.1 Add `src/components/wave2-shakeout/laneCount.ts`: a module with no import statements exporting exactly one named export `export function laneCount(lanes: readonly string[]): number` and no default export, returning the count of entries that are non-empty after `trim()` [req:5.1]. Add `src/components/wave2-shakeout/laneCount.test.ts` with plain vitest tests (no DOM) asserting `laneCount([])` is `0`, `laneCount(['a', '', '  ', 'b'])` is `2`, and `laneCount(['  a  '])` is `1` [req:5.2]. Do not mount it anywhere, do not modify `src/App.tsx`, and modify no file outside `src/components/wave2-shakeout/` [req:5.3]. Verify with `npm run verify` and `npm test`, both of which must exit zero.
  _Requirements: 5.1, 5.2, 5.3_
  _Boundary: src/components/wave2-shakeout_
  _Depends: 1.1_
